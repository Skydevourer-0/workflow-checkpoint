{
  "name": "workflow-checkpoint-dsh",
  "description": "DSH native plugin: inject pending workflow-checkpoint tasks at session start (agent/session-start hook) and expose the evidence ledger as tools (checkpoint_report / checkpoint_ledger). Reuses checkpoint.py.",
  "version": "0.2.0",
  "type": "module",
  "main": "lib/index.js",
  "license": "MIT",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-llm": "^0.1.2-alpha.1"
  }
}
