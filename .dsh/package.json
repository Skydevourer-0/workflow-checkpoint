{
  "name": "workflow-checkpoint-dsh",
  "description": "DSH native hook: inject pending workflow-checkpoint tasks at session start via the agent/session-start extension point (reuses checkpoint.py list --hook).",
  "version": "0.1.0",
  "type": "module",
  "main": "lib/index.js",
  "license": "MIT",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-llm": "^0.1.2-alpha.1"
  }
}
